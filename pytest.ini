[pytest]
pythonpath = .
testpaths = tests
; Não usamos classes de teste (só funções) -- desativa a coleta baseada em
; nome de classe para o pytest não confundir data.schemas.TestCase (um
; modelo Pydantic de domínio) com uma classe de teste.
python_classes =
